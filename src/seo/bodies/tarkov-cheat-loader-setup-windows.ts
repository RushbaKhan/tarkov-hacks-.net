export const body = `## Windows Loader Setup for Escape From Tarkov Cheats

Most **Escape From Tarkov cheats** ship through a Windows loader — a small app that verifies your license, downloads the latest module build, and launches the overlay beside EFT. Setup takes minutes when you follow the order page instructions exactly.

This guide walks through install, first launch, hotkey basics, and post-patch updates. For feature tuning after setup, read /blog/escape-from-tarkov-cheats-esp-aimbot and /blog/escape-from-tarkov-esp-guide.

## Before You Install

- Run Windows 10 or 11 with current updates.
- Disable conflicting overlay apps if the loader docs mention them.
- Download only from the link tied to your purchase — never random Discord attachments.
- Close EFT completely before first inject or attach step.

## Install and License Steps

1. Download the loader from your order confirmation page.
2. Extract to a simple folder path without special characters.
3. Run as administrator only if the vendor requires it.
4. Enter your license key when prompted and wait for module download.
5. Confirm **BattlEye** compatibility status shows green or "working" before launching raids.

## First Overlay Launch

Start the loader, then launch Escape From Tarkov from the official launcher — order may vary by product. Open the in-loader menu with the default hotkey (often Insert or Home) and verify ESP draws in hideout or offline mode if supported.

Configure basics before live raids:

- Player ESP distance cap
- Extraction ESP on
- Aim activation on hold key, not always on
- Stream-proof if you record content

## Hotkeys and Profiles

Save profiles per map — Factory CQB versus Woods DMR need different FOV and loot ranges. Reference /blog/eft-aimbot-no-recoil-guide for combat profiles and /blog/escape-from-tarkov-loot-esp-guide for profit routes.

## After Every EFT Patch

Battlestate patches shift offsets. Expect brief downtime until the loader updates.

1. Check our homepage for status before marathon sessions.
2. Run the loader updater when prompted — do not copy old DLLs manually.
3. Re-test toggles in hideout or Scav raid before taking expensive kits.

## Troubleshooting

- **Overlay not showing**: Verify game borderless or windowed mode per vendor docs; restart loader and game.
- **Features greyed out**: Patch day — wait for compatibility confirmation.
- **Crash on inject**: Re-download fresh loader; exclude false positives only if vendor instructs.

## Summary

Proper **Windows loader setup** keeps **Escape From Tarkov cheats** on supported builds with sane defaults. Download from official order paths, tune ESP before combat assists, and update after every patch. Return to our homepage for status and purchase access when ready.`;
